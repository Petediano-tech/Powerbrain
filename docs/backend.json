
{
  "entities": {
    "UserAccount": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserAccount",
      "type": "object",
      "description": "Represents a user account within the Power Brain application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user account."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "username": {
          "type": "string",
          "description": "Username chosen by the user."
        },
        "registrationDate": {
          "type": "string",
          "description": "Date and time when the user registered.",
          "format": "date-time"
        },
        "lastLogin": {
          "type": "string",
          "description": "Date and time of the user's last login.",
          "format": "date-time"
        },
        "profileId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserAccount 1:1 UserProfile)"
        }
      },
      "required": [
        "id",
        "email",
        "username",
        "registrationDate",
        "profileId"
      ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents the profile information of a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user profile."
        },
        "firstName": {
          "type": "string",
          "description": "First name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "Last name of the user."
        },
        "dateOfBirth": {
          "type": "string",
          "description": "Date of birth of the user.",
          "format": "date-time"
        },
        "profilePicture": {
          "type": "string",
          "description": "URL of the user's profile picture.",
          "format": "uri"
        },
        "gradeLevel": {
          "type": "string",
          "description": "Grade level of the user (e.g., Form 1, Std 8)."
        },
        "schoolId": {
          "type": "string",
          "description": "Reference to School. (Relationship: UserProfile 1:N School)"
        },
        "studyStreaks": {
          "type": "number",
          "description": "Number of consecutive days the user has studied."
        },
        "totalTimeStudied": {
          "type": "number",
          "description": "Total time in minutes the user has studied."
        },
        "quizzesCompleted": {
          "type": "number",
          "description": "Total number of quizzes the user has completed."
        },
        "topicsMastered": {
          "type": "number",
          "description": "Total number of topics the user has mastered."
        },
        "badges": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of badges the user has earned."
        },
        "averageScore": {
          "type": "number",
          "description": "The user's average score across all quizzes."
        },
        "subscriptionTier": {
            "type": "string",
            "enum": ["free", "vip1", "vip2", "vip3", "vip4", "power"],
            "description": "The user's current subscription tier."
        },
        "dailyChatCount": {
            "type": "number",
            "description": "Number of AI tutor questions asked today."
        },
        "lastChatDate": {
            "type": "string",
            "format": "date",
            "description": "The date of the last chat interaction, in YYYY-MM-DD format."
        }
      },
      "required": [
        "id",
        "firstName",
        "lastName",
        "gradeLevel"
      ]
    },
    "Note": {
      "title": "Note",
      "type": "object",
      "description": "Represents a single note or document as text content.",
      "properties": {
        "title": { "type": "string", "description": "Title of the note." },
        "subject": { "type": "string", "description": "Subject category of the note." },
        "author": { "type": "string", "description": "Author of the note." },
        "content": { "type": "string", "description": "The full text content of the note." },
        "imageUrl": { "type": "string", "format": "uri", "description": "URL for the cover image of the note." },
        "imageHint": { "type": "string", "description": "Hint for AI image generation." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp when the note was created." }
      },
      "required": ["title", "subject", "author", "content", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "userAccounts/{userId}": {
      "schema": {
        "$ref": "#/entities/UserAccount"
      },
      "description": "Stores user account information. Uses path-based ownership for security. Includes standard user profile information such as email, username, registration date and last login."
    },
    "userProfiles/{profileId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores user profile details. Uses document id for security (profileId field from UserAccount). Includes user's personal information like first name, last name, date of birth, profile picture, gradeLevel, and school ID."
    },
    "notes/{noteId}": {
        "schema": { "$ref": "#/entities/Note" },
        "description": "Stores metadata for uploaded notes. Publicly readable so students can browse available notes, but only authenticated teachers can create new entries."
    }
  }
}
